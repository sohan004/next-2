import React from 'react';

export const metadata = {
    title: "About page",
    description: "This is the about page",
};

const Page = () => {
    return (
        <div>
            this is about page
        </div>
    );
};

export default Page;